pipeline {
    agent any
    stages {
      stage('Scan') {
        steps {
          withSonarQubeEnv(installationName: 'Project1') {
            bat "mvnw clean org.sonarsource.scanner.maven:sonar-maven-plugin:3.9.1.2184:sonar -Dsonar.token=adminToken"
          }
        }
      }
    }
}